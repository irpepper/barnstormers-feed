# barnstormers-feed
Scrapes new posts in Barnstormers avionics categories and emails listings for the COM radios being watched (Garmin GTR 200/205, Garmin SL40, Trig TY96). Edit `DEFAULT_WATCH_PATTERNS` in `scraper.py` or set the `WATCH_PATTERNS` env var to change the watch list.
